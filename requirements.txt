# requirements.txt
# (project root)

# Core deep learning stack
torch==2.1.0
torchvision==0.16.0
torchaudio==2.1.0

# Numeric / image / utils
numpy==1.26.4        # <--- SAFE with many C-extensions (incl. open_clip)
Pillow==10.2.0
opencv-python==4.8.1.78
scikit-image==0.22.0
einops==0.7.0
tqdm==4.66.1
pyyaml==6.0.1
lpips==0.1.4

# Vision-language + extras
open_clip_torch==2.24.0
entmax==1.0

